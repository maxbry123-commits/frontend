---
"assistant-cloud": patch
"@assistant-ui/react": patch
---

feat(cloud): claim anonymous threads into a signed-in workspace by exposing the browser's anonymous refresh token and moving its threads into an authenticated Assistant Cloud workspace after sign-in.
