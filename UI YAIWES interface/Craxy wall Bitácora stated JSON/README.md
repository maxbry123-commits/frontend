# Craxy wall Bitácora stated JSON

Fuente de verdad de progreso UI.

- `state.json` — v, tab, mode, ok_ids, parte_actual, notas por bloque 00–25.
- Cada `OK ID` actualiza `ok_ids` y `notes`.
- El HTML Crazy Wall (01-original) **no se reescribe** aquí; esto es bitácora del proyecto UI.
