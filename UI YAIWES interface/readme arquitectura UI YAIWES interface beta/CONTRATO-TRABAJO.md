# CONTRATO DE TRABAJO · UI YAIWES interface

Fecha: 2026-09-08 · Repo `maxbry123-commits/frontend` · branch `main`  
Trigger para empezar a fabricar: **`CICLO RUN-01`** (nada más).

## 1. Partes

- Director: aprueba IDs, sube zip/foto/HTML, dice `OK <ID>` o `RECHAZO <ID>`.
- Agente: ejecuta el LOOP. No inventa. No unifica. No GitHub FROMTED sin `OK`.

## 2. Alcance

5 partes. 39 ítems. 24 con HTML. Anims fuera de los 39.

| Parte | Qué | GitHub FROMTED |
|-------|-----|----------------|
| P1 | RUN-01…08 | solo con `OK` |
| P2 | WALL-01…16 | solo con `OK` |
| P3 | GBOT-01 cáscara | solo con `OK` |
| P4 | FOTO-01…14 motivos | no son HTML |
| P5 | anims extra | archivos aparte |

## 3. Ley

- 1 ventana = 1 archivo.
- `01-original` = byte-idéntico a lo que subiste.
- `02-fromted` = el mismo JS/layout, solo Matte `#0a0a0d` / Little `#2563eb` / Blanco `#ffffff` / naranja `#ff5500` Cargar-Descargar.
- 1 ID por ciclo. Máximo 2 si el director lo escribe.
- Fábrica UI interna. Usuario producto no entra.
- Backend de un zip → `biblioteca-backend/01-original` identificado, nunca mezclado en el HTML.

## 4. Verdad biblioteca (2026-09-08)

**NO.** No está todo el sandbox en GitHub biblioteca.

En biblioteca FE ahora: lote-01 (10), lote-02 (10), lote-03 1 HTML review, lote-BACKEND nota, skills-claude refs, 1 nota de cableado.

En `UI YAIWES interface/…/01-original`: Run 3 HTML, Crazy Wall 3 HTML, p01–p10 + v2 + DOC1.

**Sigue en sandbox, no biblioteca:** ~30 zips, ~106 fotos, extrações AUDITORIA-ZIPS, clones, `public/` fábrica previa.

Cada zip entra al LOOP como source, no se volcó a ciegas.

## 5. Fallos que rompen el contrato

Unificar · mock · paleta naranja HUD · llamar Grok Bot a OSS · push sin `OK` · inventar sin HTML · saltarse S4/S5.

## 6. Firma operativa

El LOOP (`LOOP-OPERATIVO.md` + `SCHEMA-LOOP.json`) es obligatorio en cada ID.
