name: factory-f-fe-072-touch-gesture-arbitration

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/factory-f-fe-072-touch-gesture-arbitration.yml'
      - 'UI YAIWES/watchdog UI YAIWES/➡️ Astra plan fábrica UI YAIWES/Frontend/factory-v0/src/touch-dnd-v194.js'
      - 'UI YAIWES/watchdog UI YAIWES/➡️ Astra plan fábrica UI YAIWES/Frontend/factory-v0/tests/e2e/f-fe-072-touch-gesture-arbitration.spec.mjs'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: factory-f-fe-072-touch-gesture-arbitration
  cancel-in-progress: false

jobs:
  gesture-arbitration:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: UI YAIWES/watchdog UI YAIWES/➡️ Astra plan fábrica UI YAIWES/Frontend/factory-v0
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          lfs: false
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            UI YAIWES/watchdog UI YAIWES/➡️ Astra plan fábrica UI YAIWES/Frontend/factory-v0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Preserve prior touch versions
        run: |
          set -euo pipefail
          test -s src/touch-dnd-v192.js
          test -s src/touch-dnd-v193.js
          test -s src/touch-dnd-v194.js
          grep -F "version:'1.9.2'" src/touch-dnd-v192.js
          grep -F "TOUCH_DND_VERSION = '1.9.3'" src/touch-dnd-v193.js
          grep -F "TOUCH_DND_VERSION = '1.9.4'" src/touch-dnd-v194.js
          ! grep -F "1.9.4" src/touch-dnd-v192.js
          ! grep -F "1.9.4" src/touch-dnd-v193.js
      - name: Install harness
        run: npm install --no-audit --no-fund
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Syntax F-FE-072
        run: |
          node --check src/touch-dnd-v194.js
          node --check tests/e2e/f-fe-072-touch-gesture-arbitration.spec.mjs
      - name: F-FE-072 desktop unit arbitration
        run: npx playwright test tests/e2e/f-fe-072-touch-gesture-arbitration.spec.mjs --project=chromium-desktop --reporter=line
      - name: F-FE-072 Android-size Pixel 7 gate
        run: npx playwright test tests/e2e/f-fe-072-touch-gesture-arbitration.spec.mjs --project=mobile-chromium --reporter=line
      - name: Preserve F-FE-072 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: factory-f-fe-072-touch-gesture-arbitration-evidence
          path: |
            UI YAIWES/watchdog UI YAIWES/➡️ Astra plan fábrica UI YAIWES/Frontend/factory-v0/playwright-report
            UI YAIWES/watchdog UI YAIWES/➡️ Astra plan fábrica UI YAIWES/Frontend/factory-v0/test-results
          if-no-files-found: warn
          retention-days: 14
