name: Motor G3 Compatible 2 Retry 20260910
run-name: Motor G3 Compatible 2 Retry • ${{ github.sha }}

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/motor-g3-clean2-retry-20260910.yml'

permissions:
  contents: write

concurrency:
  group: motor-g3-compatible-2-retry-20260910
  cancel-in-progress: false

jobs:
  motor-g3-compatible:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Checkout canonical motor
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          lfs: false
          sparse-checkout: |
            ➡️📂motores de descarga extracción copiado movimiento archivos fromtend/📂Motor descarga de componentes y extracción de zip
          sparse-checkout-cone-mode: false
      - name: Verify canonical motor blobs
        shell: bash
        run: |
          set -euo pipefail
          MOTOR_DIR='➡️📂motores de descarga extracción copiado movimiento archivos fromtend/📂Motor descarga de componentes y extracción de zip'
          test "$(git hash-object "$MOTOR_DIR/motor_2_queue_download_extract.py")" = '84d566e2ee4e98e42eb3a864026d067d48caabd9'
          test "$(git hash-object "$MOTOR_DIR/hf_download_extract_engine.py")" = '91e6e4486692eab314be5c7130d8310d3c855397'
      - name: Execute jsonschema and fast-check sequentially
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          DEST_REPO: maxbry123-commits/frontend
          DEST_BRANCH: main
          DEST_ROOT: UI YAIWES/watchdog UI YAIWES/Download librerías
          MAX_RETRIES: '3'
        run: |
          set -euo pipefail
          MOTOR_DIR="$GITHUB_WORKSPACE/➡️📂motores de descarga extracción copiado movimiento archivos fromtend/📂Motor descarga de componentes y extracción de zip"
          export ENGINE_PATH="$MOTOR_DIR/hf_download_extract_engine.py"
          export QUEUE_FILE="$RUNNER_TEMP/g3-clean2.queue.json"
          export STATE_FILE="$RUNNER_TEMP/g3-clean2.state.json"
          export INDEX_PATH="$RUNNER_TEMP/g3-clean2.index.md"
          cat > "$QUEUE_FILE" <<'JSON'
          {"queue":[
            {"id":"g3-clean-jsonschema","source_repo":"python-jsonschema/jsonschema","slug":"jsonschema","publish":true},
            {"id":"g3-clean-fast-check","source_repo":"dubzzz/fast-check","slug":"fast-check","publish":true}
          ]}
          JSON
          python3 "$MOTOR_DIR/motor_2_queue_download_extract.py" | tee "$RUNNER_TEMP/g3-clean2.result.jsonl"
          cat "$STATE_FILE"
          tail -n 1 "$RUNNER_TEMP/g3-clean2.result.jsonl" > "$RUNNER_TEMP/g3-clean2.final.json"
          python3 - "$RUNNER_TEMP/g3-clean2.final.json" <<'PY'
          import json,sys
          p=json.load(open(sys.argv[1]))
          b=p.get('balance',{})
          assert p.get('verdict')=='VERIFIED_CLOSED', p
          assert b.get('failed')==0 and b.get('pending')==0, p
          assert b.get('published_readback_verified')==b.get('total')==2, p
          PY
