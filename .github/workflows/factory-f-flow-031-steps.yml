name: factory-f-flow-031-steps
on:
  push:
    branches: [main]
    paths:
      - 'UI YAIWES/watchdog UI YAIWES/➡️ Astra plan fábrica UI YAIWES/Frontend/factory-v0/tests/e2e/f-flow-031-steps.spec.mjs'
      - '.github/workflows/factory-f-flow-031-steps.yml'
  workflow_dispatch:
jobs:
  flow-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: 'UI YAIWES/watchdog UI YAIWES/➡️ Astra plan fábrica UI YAIWES/Frontend/factory-v0'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          filter: blob:none
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            UI YAIWES/watchdog UI YAIWES/➡️ Astra plan fábrica UI YAIWES/Frontend/factory-v0/
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install --no-audit --no-fund
      - run: npx playwright install --with-deps chromium
      - name: Syntax
        run: node --check tests/e2e/f-flow-031-steps.spec.mjs
      - name: F-FLOW-031 browser gate
        run: npx playwright test tests/e2e/f-flow-031-steps.spec.mjs --project=chromium-desktop --reporter=line
