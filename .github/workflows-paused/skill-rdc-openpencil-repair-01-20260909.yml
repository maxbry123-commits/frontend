name: Skill RDC OpenPencil Repair 01 20260909
run-name: Skill RDC OpenPencil Repair 01 20260909 • ${{ github.event_name }} • ${{ github.sha }}

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/skill-rdc-openpencil-repair-01-20260909.yml'

permissions:
  contents: write

env:
  GIT_TERMINAL_PROMPT: "0"

concurrency:
  group: maxbry123-commits-frontend-componentes-open-soure-fromtend
  cancel-in-progress: false

jobs:
  download-1-in-queue:
    name: Download 1 repo sequentially
    runs-on: ubuntu-latest
    timeout-minutes: 330
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          lfs: false
      - name: Execute deterministic download chain
        shell: bash
        env:
          GIT_TERMINAL_PROMPT: "0"
        run: |
          set -euo pipefail
          # Git LFS prohibited: never invoke git-lfs. Inherited lfs filters pass bytes unchanged.
          git config --local filter.lfs.clean cat
          git config --local filter.lfs.smudge cat
          git config --local filter.lfs.process ''
          git config --local filter.lfs.required false
          git config --local core.compression 0
          python3 scripts/skill-rdc-openpencil-repair-01-20260909/research_download_chain.py '📂componentes open soure fromtend/OpenPencil' '_work/skill-rdc-openpencil-repair-01-20260909'
