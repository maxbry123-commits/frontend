# Check if python (without the 3) works
python --version

# Check if it's in the PATH via py launcher
py --version

# Check if pip exists
pip --version

# Check all possible Python locations
where.exe python
where.exe py