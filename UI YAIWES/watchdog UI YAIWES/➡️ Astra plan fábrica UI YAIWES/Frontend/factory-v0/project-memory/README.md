# Project Memory — YAIWES UI Factory

Purpose: canonical destination for every factory module/work artifact created by `➡️ Astra plan fábrica UI YAIWES` before promotion elsewhere.

Rules:
- one task = one node;
- max 3 execution steps per node;
- every artifact records owner, source SHA, destination, evidence and status;
- no secret values; only `secret_ref`;
- T1_FACTORY_FRONTEND remains the gate; T2 product work is not promoted until T1 VERIFIED_CLOSED;
- Frontend and Backend outputs remain explicitly separated;
- backend code discovered from OSS is staged for later cable/integration and does not silently overwrite Sol-owned backend paths.

Current memory sections:
- `sol-nodes/` — work schemas claimed by Sol environments;
- future module folders — one folder per created module/work unit;
- evidence references remain linked back to STATE/CHECKPOINT/Crazy Wall.
