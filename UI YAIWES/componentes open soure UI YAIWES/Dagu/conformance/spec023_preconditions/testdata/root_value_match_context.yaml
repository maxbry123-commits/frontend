working_dir: rootctx
env:
  READY_VALUE: root-ready
preconditions:
  - condition: "$READY_VALUE"
    expected: root-ready
steps:
  - id: main
    run: printf '%s\n' root-context > root-context-ran.txt
