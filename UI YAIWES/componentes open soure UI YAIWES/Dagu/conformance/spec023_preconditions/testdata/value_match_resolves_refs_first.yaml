working_dir: .
params:
  - name: environment
    type: string
    default: prod
steps:
  - id: write
    preconditions:
      - condition: "${params.environment}-api"
        expected: prod-api
    run: printf '%s\n' refs > refs-first.txt
