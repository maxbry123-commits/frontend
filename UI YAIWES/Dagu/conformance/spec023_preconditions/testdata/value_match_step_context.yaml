working_dir: .
steps:
  - id: write
    working_dir: workspace
    env:
      READY_VALUE: ready
    preconditions:
      - condition: "$READY_VALUE"
        expected: ready
    run: printf '%s\n' context > context-ran.txt
