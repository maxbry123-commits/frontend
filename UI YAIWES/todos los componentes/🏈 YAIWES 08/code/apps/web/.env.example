# Data source. "mock" needs no backend and streams demo data.
# Switch to "http" when the real API is up. That is the only change needed.
VITE_API_MODE=mock

# Used only when VITE_API_MODE=http
VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_WS_URL=ws://localhost:8080/api/v1/ws
