[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "yaiwes-wordflow-loop"
version = "0.1.0"
description = "Layered fail-closed Wordflow LOOP runtime for YAIWES"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
