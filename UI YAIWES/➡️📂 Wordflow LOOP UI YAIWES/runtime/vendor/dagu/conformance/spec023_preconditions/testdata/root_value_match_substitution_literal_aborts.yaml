name: root_substitution_literal
working_dir: .
preconditions:
  - condition: '$(printf "%s\n" root-error > root-substitution-ran.txt; exit 7)'
    expected: ready
handler_on:
  init:
    run: printf '%s\n' init > init-ran.txt
  abort:
    run: printf '%s\n' abort > abort-ran.txt
  failure:
    run: printf '%s\n' failure > failure-ran.txt
steps:
  - id: main
    run: printf '%s\n' main > main-ran.txt
