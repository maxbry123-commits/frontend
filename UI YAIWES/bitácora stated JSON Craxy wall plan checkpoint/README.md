# UI YAIWES — Bitácora / STATE / Crazy Wall / Plan / Checkpoint

Contrato: `tel.workflow/v3`
Modo: `FAIL_CLOSED_LOOP`

Esta raíz concentra desde este nodo los artefactos operativos solicitados por el Director: bitácora, STATE, CHECKPOINT, PLAN, RECOVERY y evidencia de inventario. No reemplaza las anclas históricas del Wordflow; las referencia y conserva trazabilidad.

Orden de trabajo: `INPUT literal → GOALS → prioridades → plan → cola 1×1 → verify/refute → GAP→RESEARCH→StrategyDelta distinto→mismo nodo → Council12 → refutaciones → cross-check → CODA → verify_final`.

Reglas: código no monolítico; componentes centralizados en `UI YAIWES/componentes open soure UI YAIWES/`; el Wordflow recibe solo módulos/código necesario mediante adapters/plugins; archivos upstream de CI/docs/changelog no se copian al runtime; licencias/provenance no se consideran basura y deben preservarse por referencia/evidencia.